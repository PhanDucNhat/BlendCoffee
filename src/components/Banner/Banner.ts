// export function nextSlide(current: number, length: number): number {
//   return (current + 1) % length;
// }

// export function prevSlide(current: number, length: number): number {
//   return (current - 1 + length) % length;
// }

export function goToSlide(index: number): number {
  return index;
}
